#include "main.h"
#include <stdio.h>
/**
 * main - prints program name
 * @argc: no. of arguments
 * @argv: array of arguments
 * Return: 0 success
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
