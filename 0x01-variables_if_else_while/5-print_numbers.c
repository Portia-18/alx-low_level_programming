#include <stdio.h>

/**
 * main - executes program
 *
 * putchar - writes to stdo
 *
 * for - repeats code
 *
 * Return: always 0
*/
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	putchar(i);
	putchar('\n');
	return (0);
}
